#!/bin/bash

# install.sh
# Installs minikube and tekton-cli.
# Only run this script if your system doesn't currently have minikube and tekton-cli installed.

/usr/local/bin/brew install minikube
/usr/local/bin/brew install tektoncd-cli
